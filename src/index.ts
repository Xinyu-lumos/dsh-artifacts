/** Host-side Cordis service dependencies. */
export const inject: readonly string[] = [];

/** Register the host half of the artifacts plugin. */
export function apply(_ctx: unknown): void {
  // Feature behavior begins in Task 2.
}
