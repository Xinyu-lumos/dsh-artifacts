import { ArtifactController } from "./artifact-controller.js";
/** One shared drawer state per client bundle (single browser module instance). */
export declare const artifactController: ArtifactController;
